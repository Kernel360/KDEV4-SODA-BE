input {
  file {
    path => "/usr/share/logstash/logs/*.json"
    start_position => "beginning"
    sincedb_path => "/dev/null"
    codec => "json"
  }
}

output {
  mongodb {
    uri => "mongodb+srv://admin:rulerule12@cluster0.zsqrqli.mongodb.net"
    database => "log"
    collection => "system_log"
    codec => "json"
  }
}
